#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../is-base64/bin/is-base64" "$@"
  ret=$?
else 
  node  "$basedir/../../../is-base64/bin/is-base64" "$@"
  ret=$?
fi
exit $ret
